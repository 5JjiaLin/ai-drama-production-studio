# GitHub 版本管理设置指南

## ✅ 已完成的本地配置

- ✅ Git 仓库已初始化
- ✅ .gitignore 已创建（保护 API Key 等敏感信息）
- ✅ 初始提交已完成（37 个文件，7414 行代码）

## 📋 接下来的步骤

### 1️⃣ 在 GitHub 上创建仓库

1. 访问 [GitHub](https://github.com) 并登录
2. 点击右上角的 `+` 号，选择 `New repository`
3. 填写仓库信息：
   - **Repository name**: `ai-screenplay-breaker`（或你喜欢的名字）
   - **Description**: `AI驱动的剧本自动化分镜工具`
   - **Public/Private**: 根据需要选择
   - **不要**勾选 "Initialize this repository with a README"
4. 点击 `Create repository`

### 2️⃣ 连接本地仓库到 GitHub

创建完仓库后，GitHub 会显示一些命令。在项目目录运行：

```bash
# 添加远程仓库（将 YOUR_USERNAME 替换为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/ai-screenplay-breaker.git

# 推送到 GitHub
git push -u origin master
```

### 3️⃣ 验证推送成功

刷新 GitHub 页面，你应该能看到所有文件已上传。

## 🔄 日常使用流程

### 提交更改

```bash
# 查看修改的文件
git status

# 添加修改的文件
git add .

# 提交更改（附上有意义的提交信息）
git commit -m "描述你的更改"

# 推送到 GitHub
git push
```

### 提交信息规范建议

使用语义化提交信息：

- `feat:` - 新功能
  - 例：`git commit -m "feat: 添加分镜导出为 PDF 功能"`
  
- `fix:` - 修复 bug
  - 例：`git commit -m "fix: 修复 DeepSeek API 解析错误"`
  
- `docs:` - 文档更新
  - 例：`git commit -m "docs: 更新 README 使用说明"`
  
- `style:` - 代码格式调整（不影响功能）
  - 例：`git commit -m "style: 统一代码缩进"`
  
- `refactor:` - 重构代码
  - 例：`git commit -m "refactor: 优化分段生成逻辑"`
  
- `perf:` - 性能优化
  - 例：`git commit -m "perf: 减少 API 调用次数"`
  
- `test:` - 测试相关
  - 例：`git commit -m "test: 添加单元测试"`
  
- `chore:` - 构建/工具相关
  - 例：`git commit -m "chore: 更新依赖包版本"`

### 快速提交流程示例

```bash
# 1. 修改了一些文件后
git status

# 2. 添加所有更改
git add -A

# 3. 提交
git commit -m "fix: 修复分镜生成时的 JSON 解析问题"

# 4. 推送到 GitHub
git push
```

## 🔐 安全提示

### ⚠️ 重要：保护你的 API Key

.gitignore 文件已配置排除以下内容：

- `apikey.txt` - API Key 文件
- `.env` - 环境变量文件
- `*.key` - 所有 .key 文件

**永远不要**将包含 API Key 的文件提交到 GitHub！

### 检查是否意外提交了敏感信息

```bash
# 查看即将提交的文件
git status

# 如果发现敏感文件，移除它
git reset HEAD apikey.txt
```

## 📊 查看提交历史

```bash
# 查看提交日志
git log

# 查看简洁日志
git log --oneline

# 查看最近 5 条提交
git log -5
```

## 🔄 拉取远程更改（多人协作时）

```bash
# 拉取最新更改
git pull
```

## 🌿 分支管理（进阶）

```bash
# 创建新分支
git checkout -b feature/新功能名

# 切换分支
git checkout master

# 合并分支
git merge feature/新功能名

# 推送分支到 GitHub
git push -u origin feature/新功能名
```

## 🆘 常见问题

### 忘记添加文件就提交了

```bash
# 添加遗漏的文件
git add 遗漏的文件.txt

# 修改最后一次提交
git commit --amend
```

### 提交信息写错了

```bash
# 修改最后一次提交信息
git commit --amend -m "正确的提交信息"
```

### 撤销未提交的更改

```bash
# 撤销所有未提交的更改（危险！）
git reset --hard HEAD
```

## 📚 更多资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub 使用指南](https://docs.github.com/cn)
- [Git 简明教程](https://www.runoob.com/git/git-tutorial.html)

---

**下一步**: 按照上面的步骤在 GitHub 创建仓库并连接！
